extends layout

block extraHead
  link(rel='stylesheet', href='/stylesheets/style.css')

  script(src="/javascripts/index.js")
  script.
    var ROOT_URL = "#{rootUrl}";
    var SOCKET_PORT = "#{socketPort}";
    var socket = io.connect(ROOT_URL + ':' + SOCKET_PORT);

block content
  div.header
    h1 this is <span id="secret-reset" class="red">digital</span> dilemma.
    h1 welcome.

  div.col-container
    div.left
      div#grid

    div.right
      b Instructions
      div#instructions
      br
      a#start-button.button start
      a#digital-reset-button.button reset path
      br
      br
      b Path
      div#path

    br.clear
